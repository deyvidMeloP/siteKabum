// Função TesteHello
function swiperProductMain() {

  const swiper = new Swiper('.item_Selection', {
    // Optional parameters
    
    slidesPerView: 5,
    spaceBetween: 6,
    direction: 'vertical',
    // If we need pagination
 
  
    // Navigation arrows
    navigation: {
      nextEl: '.arrow_Back',
      prevEl: '.arrow_Top',
    },
  

  });
    }